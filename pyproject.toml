[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "juslag"
version = "0.1.0"
description = "Japan-U.S. sector lead-lag strategy using subspace-regularized PCA"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.7",
  "numpy>=1.26",
  "pandas>=2.2",
  "pandas-market-calendars>=4.4",
  "PyYAML>=6.0",
  "requests>=2.31",
  "scipy>=1.12",
  "yfinance>=0.2.54",
]

[project.optional-dependencies]
turso = [
  "libsql==0.1.11",
  "pyturso==0.7.2",
]
dev = [
  "playwright>=1.55",
  "python-dotenv>=1.0",
  "pytest>=8.0",
  "ruff>=0.6",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
